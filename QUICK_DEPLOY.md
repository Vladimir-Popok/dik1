# Быстрый деплой без установки CLI

## 🚀 Самый простой способ - через веб-интерфейс Vercel

### Шаг 1: Подготовьте проект для GitHub

```bash
# Если ещё не инициализирован git
git init
git add .
git commit -m "Initial commit"

# Создайте репозиторий на GitHub через веб-интерфейс, затем:
git remote add origin https://github.com/ваш-username/название-репозитория.git
git branch -M main
git push -u origin main
```

### Шаг 2: Деплой на Vercel

1. Зайдите на **[vercel.com](https://vercel.com)**
2. Войдите через GitHub
3. Нажмите **"Add New Project"**
4. Выберите ваш репозиторий
5. Нажмите **"Deploy"**

**Готово!** Сайт будет доступен через несколько секунд.

---

## 📦 Альтернатива: Деплой через npx (без sudo)

Если хотите использовать CLI, но не хотите устанавливать глобально:

```bash
# Просто используйте npx (не требует прав администратора)
npx vercel

# Следуйте инструкциям в терминале
```

---

## ✅ Проверка перед деплоем

Убедитесь, что проект собирается:

```bash
npm run build
npm run preview
```

Если всё работает локально - можно деплоить!

---

## 🔗 После деплоя

Vercel автоматически:
- ✅ Создаст URL вида `ваш-проект.vercel.app`
- ✅ Настроит автоматический деплой при каждом push в GitHub
- ✅ Выдаст бесплатный SSL сертификат
- ✅ Настроит CDN для быстрой загрузки

