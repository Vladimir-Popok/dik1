# Инструкция по деплою проекта

Этот проект можно задеплоить на множество бесплатных платформ. Ниже приведены инструкции для самых популярных вариантов.

## 🚀 Вариант 1: Vercel (Рекомендуется - самый простой)

**Vercel** - лучший выбор для Vite приложений, автоматически определяет настройки.

### Шаги:

1. **Создайте аккаунт на [vercel.com](https://vercel.com)** (можно через GitHub)

2. **Деплой через веб-интерфейс (рекомендуется):**
   - Зайдите на vercel.com
   - Нажмите "Add New Project"
   - Подключите ваш GitHub репозиторий (или загрузите файлы)
   - Vercel автоматически определит Vite и настроит всё
   - Нажмите "Deploy"

3. **Или через CLI без глобальной установки:**
```bash
# Используйте npx (не требует sudo)
npx vercel

# Или установите локально в проект
npm install --save-dev vercel
npx vercel
```

4. **Или через веб-интерфейс:**
   - Зайдите на vercel.com
   - Нажмите "Add New Project"
   - Подключите ваш GitHub репозиторий (или загрузите файлы)
   - Vercel автоматически определит Vite и настроит всё
   - Нажмите "Deploy"

**Преимущества:**
- ✅ Автоматический деплой при push в GitHub
- ✅ Бесплатный SSL сертификат
- ✅ Быстрая CDN
- ✅ Автоматические preview для pull requests

---

## 🌐 Вариант 2: Netlify

### Шаги:

1. **Создайте аккаунт на [netlify.com](https://netlify.com)**

2. **Создайте файл `netlify.toml` в корне проекта:**
```toml
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

3. **Деплой:**
   - Зайдите на netlify.com
   - Нажмите "Add new site" → "Import an existing project"
   - Подключите GitHub репозиторий
   - Настройки определятся автоматически
   - Нажмите "Deploy site"

**Или через CLI:**
```bash
npm install -g netlify-cli
netlify deploy --prod
```

---

## 📦 Вариант 3: GitHub Pages

### Шаги:

1. **Установите gh-pages:**
```bash
npm install --save-dev gh-pages
```

2. **Добавьте скрипты в `package.json`:**
```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "homepage": "https://ваш-username.github.io/название-репозитория"
}
```

3. **Обновите `vite.config.js`** (уже сделано):
```js
export default defineConfig({
  plugins: [react()],
  base: '/название-репозитория/', // Замените на название вашего репозитория
})
```

4. **Деплой:**
```bash
npm run deploy
```

5. **Включите GitHub Pages:**
   - Зайдите в Settings → Pages вашего репозитория
   - Выберите source: `gh-pages` branch
   - Сайт будет доступен по адресу: `https://ваш-username.github.io/название-репозитория`

---

## ☁️ Вариант 4: Cloudflare Pages

### Шаги:

1. **Создайте аккаунт на [cloudflare.com](https://cloudflare.com)**

2. **Деплой:**
   - Зайдите в Cloudflare Dashboard → Pages
   - Нажмите "Create a project"
   - Подключите GitHub репозиторий
   - Настройки сборки:
     - Build command: `npm run build`
     - Build output directory: `dist`
   - Нажмите "Save and Deploy"

**Преимущества:**
- ✅ Очень быстрая CDN
- ✅ Бесплатный SSL
- ✅ Автоматический деплой

---

## 🔥 Вариант 5: Firebase Hosting

### Шаги:

1. **Установите Firebase CLI:**
```bash
npm install -g firebase-tools
```

2. **Войдите в Firebase:**
```bash
firebase login
```

3. **Инициализируйте проект:**
```bash
firebase init hosting
```
   - Выберите "Use an existing project" или создайте новый
   - Public directory: `dist`
   - Single-page app: `Yes`
   - GitHub автоматические деплои: `Yes` (опционально)

4. **Создайте файл `firebase.json`** (если не создался автоматически):
```json
{
  "hosting": {
    "public": "dist",
    "ignore": [
      "firebase.json",
      "**/.*",
      "**/node_modules/**"
    ],
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
```

5. **Деплой:**
```bash
npm run build
firebase deploy
```

---

## 📋 Быстрый старт (Vercel - самый простой способ)

1. **Создайте репозиторий на GitHub:**
```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ваш-username/название-репозитория.git
git push -u origin main
```

2. **Зайдите на [vercel.com](https://vercel.com)**
   - Войдите через GitHub
   - Нажмите "Add New Project"
   - Выберите ваш репозиторий
   - Нажмите "Deploy"
   - Готово! Сайт будет доступен через несколько секунд

---

## 🔧 Локальная проверка перед деплоем

Перед деплоем убедитесь, что всё работает:

```bash
# Установите зависимости
npm install

# Соберите проект
npm run build

# Проверьте локально
npm run preview
```

Откройте `http://localhost:4173` и проверьте, что всё работает корректно.

---

## 💡 Рекомендации

- **Для начинающих:** Vercel - самый простой вариант
- **Для максимальной скорости:** Cloudflare Pages
- **Если уже используете GitHub:** GitHub Pages
- **Для интеграции с другими сервисами Google:** Firebase Hosting

Все перечисленные платформы предоставляют:
- ✅ Бесплатный хостинг
- ✅ Бесплатный SSL сертификат
- ✅ Автоматический деплой из GitHub
- ✅ Кастомный домен (опционально)

